public class TicTacToeTest {
    public static void main(String[] args) {
        GameSession session = new GameSession(); // Start a new game session
        session.startSession(); // Begin the game session
    }
}